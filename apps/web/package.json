{
  "name": "just-inspector-web",
  "private": true,
  "version": "0.1.0",
  "description": "Standalone browser build of the Just Inspector UI (connects to the game's WebSocket server directly).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@just-inspector/client": "*",
    "@just-inspector/inspector-ui": "*",
    "@just-inspector/protocol": "*",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "typescript": "^5.9.0",
    "vite": "^6.3.5"
  }
}
