{
  "name": "just-inspector",
  "private": true,
  "version": "0.1.0",
  "description": "Standalone runtime inspector UI for Godot / Bevy games — JSON WebSocket protocol, Vue 3 UI, browser + VS Code packaging.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/vscode-extension/webview"
  ],
  "scripts": {
    "build": "npm run build -w @just-inspector/protocol && npm run build -w @just-inspector/client && npm run build -w @just-inspector/inspector-ui && npm run build -w just-inspector-web && npm run build -w just-inspector-vscode",
    "build:packages": "npm run build -w @just-inspector/protocol && npm run build -w @just-inspector/client && npm run build -w @just-inspector/inspector-ui",
    "dev:web": "npm run dev -w just-inspector-web",
    "mock": "npm run start -w just-inspector-mock-server",
    "smoke": "npm run smoke -w just-inspector-mock-server",
    "package:vscode": "npm run package -w just-inspector-vscode"
  },
  "engines": {
    "node": ">=20"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.28.2": true,
    "esbuild@0.27.7": true,
    "fsevents@2.3.3": true
  },
  "devDependencies": {
    "vite-plugin-singlefile": "^2.3.3"
  }
}
